# Fix for Rollup native dependencies on Railway
optional=false
fund=false
audit=false

# Ensure all optional dependencies are installed
include=optional

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true 